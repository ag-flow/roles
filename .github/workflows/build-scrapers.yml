name: build-scrapers

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch: {}

env:
  REGISTRY: ghcr.io
  IMAGE_OWNER: ${{ github.repository_owner }}

jobs:
  build-base:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      base_image_sha: ${{ steps.tags.outputs.base_image_sha }}
    steps:
      - uses: actions/checkout@v4
      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3
      - name: Compute tags
        id: tags
        run: |
          OWNER_LC="${IMAGE_OWNER,,}"
          IMAGE="${REGISTRY}/${OWNER_LC}/agflow-scraper-base"
          BASE_SHA="${IMAGE}:sha-${GITHUB_SHA:0:7}"
          TAGS="${BASE_SHA}"
          if [[ "${GITHUB_REF}" == "refs/heads/main" ]]; then TAGS="${TAGS},${IMAGE}:latest"; fi
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then TAGS="${TAGS},${IMAGE}:${GITHUB_REF_NAME}"; fi
          echo "tags=${TAGS}" >> "$GITHUB_OUTPUT"
          echo "base_image_sha=${BASE_SHA}" >> "$GITHUB_OUTPUT"
      - name: Build & push base
        uses: docker/build-push-action@v6
        with:
          context: docker/scrapers
          file: docker/scrapers/base/Dockerfile
          push: true
          tags: ${{ steps.tags.outputs.tags }}

  build-platforms:
    needs: build-base
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      matrix:
        platform: [youtube, instagram, tiktok]
    steps:
      - uses: actions/checkout@v4
      - name: Login GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Set up Buildx
        uses: docker/setup-buildx-action@v3
      - name: Compute tags
        id: tags
        run: |
          OWNER_LC="${IMAGE_OWNER,,}"
          IMAGE="${REGISTRY}/${OWNER_LC}/agflow-scraper-${{ matrix.platform }}"
          TAGS="${IMAGE}:sha-${GITHUB_SHA:0:7}"
          if [[ "${GITHUB_REF}" == "refs/heads/main" ]]; then TAGS="${TAGS},${IMAGE}:latest"; fi
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then TAGS="${TAGS},${IMAGE}:${GITHUB_REF_NAME}"; fi
          echo "tags=${TAGS}" >> "$GITHUB_OUTPUT"
      - name: Build & push ${{ matrix.platform }}
        uses: docker/build-push-action@v6
        with:
          context: docker/scrapers/${{ matrix.platform }}
          file: docker/scrapers/${{ matrix.platform }}/Dockerfile
          push: true
          pull: true
          build-args: |
            BASE_IMAGE=${{ needs.build-base.outputs.base_image_sha }}
          tags: ${{ steps.tags.outputs.tags }}
