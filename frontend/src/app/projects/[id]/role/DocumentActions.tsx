'use client';

import type { RoleDocument } from '@/lib/types';

interface Props {
  doc: RoleDocument;
  onLockChange: () => void;
  onRegenerated: () => void;
}

/**
 * Stub D2.2 — implémentation complète (lock/unlock + regenerate) en D2.4.
 */
export function DocumentActions(_props: Props) {
  return null;
}
